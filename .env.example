# Sesión: firma de la cookie HttpOnly (genera una con: openssl rand -base64 32)
SESSION_SECRET=

# Supabase Auth (opcional). Sin estos valores la app usa el modo demo local:
# cualquier correo + contraseña (>= 6 caracteres) crea una sesión.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=